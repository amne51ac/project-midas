/** @deprecated Import from `../seo/buildSeo` — kept for existing imports. */
export {
  SITE,
  absoluteUrl,
  buildDefaultDescription,
  buildJsonLd,
  buildKeywords,
  getPageMeta,
  getSiteStats,
  type PageMeta,
  type SiteStats,
} from '../seo/buildSeo';

/** @deprecated Use buildJsonLd() */
export { buildJsonLd as siteJsonLd } from '../seo/buildSeo';
