# GitLab Pages — static site from web/

image: node:20

pages:
  stage: deploy
  script:
    - cd web
    - npm ci
    - VITE_BASE_PATH=/${CI_PROJECT_NAME}/ npm run build
    - mv dist ../public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
